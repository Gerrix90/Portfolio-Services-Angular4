/* import { InputValidsDirective } from './input-valids.directive';
import { Directive , ElementRef , HostListener, Input } from '@angular/core';

describe('InputValidsDirective', () => {
  it('should create an instance', () => {
    const directive = new InputValidsDirective()
    expect(directive).toBeTruthy();
  });
});
  */