import { ClickEnterDirective } from './click-enter.directive';

describe('HoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickEnterDirective();
    expect(directive).toBeTruthy();
  });
});
